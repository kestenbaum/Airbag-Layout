const SELECTORS = {
  HEADER_BURGER: '.header-burger',
  HEADER_MENU_ACTIVE: '.header-menu--active',
  HEADER_CONTACT: '.header-contact'
}

const DOM_ELEMENT = {
   BURGER_BTN: getDomElement(SELECTORS.HEADER_BURGER),
   HEADER_MENU: getDomElement(SELECTORS.HEADER_CONTACT)
}
